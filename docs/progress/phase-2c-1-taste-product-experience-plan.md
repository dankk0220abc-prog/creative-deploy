# Phase 2C — Unified Frontend Experience and Private Publication Plan

Date: 2026-08-02
Role: UI refinement engineer; not independent reviewer or Git sealer

## Skill Authority

`FRONTEND_SKILLS_LOADED`

Loaded this turn:

- `redesign-existing-projects` — primary audit-first direction.
- `gpt-taste` — typography, hierarchy, interaction, and anti-slop cross-check.
- `impeccable` v4.0.4 — scoped audit/detect and craft-floor polish for `apps/web/src`.

`design-taste-frontend` is intentionally out of scope because PaintPilot is an authenticated
multi-step working product, not a brand or landing page.

## Entry Baseline and Candidate Continuity

- Branch `main`; HEAD `ce6b002a8f310e5fd7f50c3086aad88d04c65eb5`.
- Tree `8b5e2678d65554232c983283d6af906b82d7c5f4`; commit count `24`.
- Staged paths `0`; Alembic head `2b1c4d5e6f70`.
- The entry worktree was intentionally not clean: it already held the nine-Web-path,
  non-Skill-backed Phase 2C-1 Candidate plus its untracked plan/evidence/manifest set.
- That Candidate was read before work began and kept in place. No `reset`, `checkout`, `clean`,
  deletion, or from-scratch replacement was performed.

## Incremental Refinement Plan

1. Audit the real login, Projects, Project detail, reviewer, upload/readiness, Region/Polygon,
   history, and review surfaces with the loaded Taste Skills.
2. Retain the existing charcoal, warm-paper, oxidized-copper, and mineral-teal direction while
   improving the ordering of state, next action, and supporting facts.
3. Add a single typed `en-US` / `zh-CN` resource layer with saved/browser/config/fallback locale
   resolution, a global pre/post-login switcher, synchronized document language/title, localized
   enum display maps, and `Intl` date/relative-time/number formatting.
4. Cover the complete product-owned UI copy while preserving user values, filenames, names,
   identifiers, hashes, object keys, logs, and raw API values exactly as data.
5. Apply audit-confirmed bilingual length, overflow, hierarchy, and responsive-composition
   adjustments without changing governed product behavior.
6. Run focused localization tests and the complete Web lint/type/test/build gates. Exercise an
   English create flow, in-place Chinese switching, Region state, and 390/768/1440 responsive
   views in the browser without repeating the Docker staging journey.
7. Prepare README, environment guidance, ignore rules, security notes, synthetic UI imagery,
   and GitHub Private description/topics without adding a License or changing repository hosting.
8. Update the Candidate, audit direction, command ledger, browser evidence, and manifest without
   bringing Skill files, `.agents`, `skills-lock.json`, dependencies, or build output into the
   Candidate.

## Non-goals and Stop Conditions

- No API, OIDC/session, authorization, workflow, database, migration, private storage,
  backup/restore, staging/TLS, AI, OCR, Agent, or RAG change.
- No landing-page Hero/AIDA, bento template, carousel, scroll spectacle, external font,
  generated asset, or design-system dependency introduced into the workbench.
- No independent approval, Git sealing, staging, commit, push, tag, pull request, remote creation,
  public visibility change, or GitHub publication.
